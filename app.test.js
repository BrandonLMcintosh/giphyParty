define('getGif()', () => {
    
});

describe('appendGif()', () => {

    it('should append a gif to the page', () => {

    });

    it('should handle multiple gifs appended to the page', () => {

    });
});
    
describe('removeGifs()', () => {
    it('should remove a single gif from a page if only one exists', () => {

    });

    it('should remove multiple gifs from the page if multiple exist', () => {

    });

    it('should still run even if there are no gifs on the page', () => {

    });
});
    